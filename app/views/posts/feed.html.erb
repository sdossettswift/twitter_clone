<div class="list-group">
  <% @posts.each do |post| %>
    <div class="list-group-item">
      <%= link_to post.name, post_path(id: post.id) %> - By <%= post.user.username %>
    </div>
  <% end %>

</div>
